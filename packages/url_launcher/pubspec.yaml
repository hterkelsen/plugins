name: url_launcher
description: Flutter plugin for launching a URL on Android and iOS. Supports
  web, phone, SMS, and email schemes.
author: Flutter Team <flutter-dev@googlegroups.com>
homepage: https://github.com/flutter/plugins/tree/master/packages/url_launcher
version: 5.1.2

flutter:
  plugin:
    androidPackage: io.flutter.plugins.urllauncher
    iosPrefix: FLT
    webFilename: url_launcher_web.dart
    pluginClass: UrlLauncherPlugin

dependencies:
  flutter:
    sdk: flutter
  flutter_web_shell:
    path: /usr/local/google/home/het/Projects/engine/src/flutter/flutter_web_shell

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.0.0-dev.28.0 <3.0.0"
  flutter: ">=1.5.0 <2.0.0"
